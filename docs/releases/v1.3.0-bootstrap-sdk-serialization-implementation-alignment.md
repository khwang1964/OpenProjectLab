# v1.3.0 Bootstrap SDK Serialization Implementation Alignment

> **Status:** Implemented --- Awaiting post-merge acceptance closure
> **Implementation PR:** #265
> **Implementation merge:** `0407b4986d60578183546e98f5dc57aff890f4a7`

## Implemented scope

The minimum `generator.sdk.bootstrap_serialization` module now provides the accepted
`opl.bootstrap/1.0` envelope, canonical UTF-8 JSON, request/result/evidence round-trip,
forward-slash lexical paths, strict fail-closed decoding, and non-executing decode.

Existing `generator.sdk` root exports remain unchanged. Plugin validation checks, binary
formats, streaming, schema migration, signing, encryption, remote execution, async
transport, and automatic recovery remain deferred.

## Confirmed implementation evidence

```text
Implementation PR --- #265
Implementation merge --- 0407b4986d60578183546e98f5dc57aff890f4a7
Required implementation CI --- Passed
Synchronized main identity --- Verified
Post-merge focused verification --- 30 passed
Full regression --- 2551 passed, 56 skipped, 1 deselected
Coverage --- 90.94%
```

This alignment record does not declare implementation acceptance. A separate acceptance
closure PR must pass its own focused tests, pre-commit, full regression, coverage gate,
and required CI.

```text
Production implementation --- Implemented / Awaiting implementation acceptance
Implementation acceptance --- Pending separate closure
```
