# OpenProjectLab v1.0 RC Acceptance Record

> **Status:** Accepted
> **Milestone:** 8 — v1.0 Stabilization & Release Readiness
> **Step:** 8.10.9 — Formal RC Acceptance / Post-merge Consistency
> **Date:** 2026-08-19
> **Target RC:** `v1.0.0-rc.1`
> **Python package version:** `1.0.0rc1`
> **Governing contract:** `docs/releases/v1.0-rc-acceptance.md`
> **Publication contract:** `docs/releases/v1.0-rc-creation-publication-identity.md`

---

## 1. Purpose

This record captures the actual evidence used to decide whether
OpenProjectLab `v1.0.0-rc.1` may be formally Accepted.

It is an acceptance record, not a governing design.

The governing acceptance contract remains:

```text
docs/releases/v1.0-rc-acceptance.md
```

The publication identity contract remains:

```text
docs/releases/v1.0-rc-creation-publication-identity.md
```

This record must fail closed. It must not state that the RC is formally
Accepted until the final acceptance PR, CI, merge, synchronized `main`, and
post-merge consistency gates are complete.

---

## 2. Acceptance Decision State

Current decision:

```text
Formal RC Acceptance --- Accepted
```

Final closure state:

```text
Acceptance PR --- #154
Acceptance PR CI --- Passed
Acceptance squash merge --- Completed
Acceptance merge commit --- d37a3d84161e66e98ebbff2aafaf1a14e27f865c
main synchronization --- Completed
post-merge consistency verification --- Completed
cross-document terminal-state alignment --- Completed
```

Therefore this record is formally **Accepted**.

---

## 3. Prerequisite

Step 8.9 — Full Release-readiness Verification is formally Accepted.

Required prerequisite:

```text
Step 8.9 --- Accepted
```

Step 8.10 RC Acceptance remains a separate gate from Step 8.9 and from
`v1.0.0` GA Acceptance.

---

## 4. Step 8.10 Delivery State

Current Step 8.10 delivery sequence:

```text
8.10.1 RC Acceptance Baseline                   Completed
8.10.2 RC Acceptance Contract                   Completed
8.10.3 RC Contract Automation                   Completed
8.10.4 RC Build / Artifact Identity             Completed
8.10.5 RC Artifact-backed Verification          Completed
8.10.6 RC Full Regression / Local Quality Gates Completed
8.10.7 RC GitHub Actions / CI                   Completed
8.10.8 RC Creation / Publication Identity       Completed
8.10.9 Formal RC Acceptance / Post-merge        Completed
```

No prior Step 8.10 responsibility is omitted from this decision.

---

## 5. Canonical RC Identity

Accepted candidate identity:

```text
Human-facing RC identity --- v1.0.0-rc.1
Python package version --- 1.0.0rc1
```

The package/release normalization boundary is intentional:

```text
1.0.0rc1
    ↕
v1.0.0-rc.1
```

This record does not introduce `v1.0.0` GA identity.

---

## 6. Approved and Published Source Commit

Approved publication commit:

```text
b5958edbbf0e3279ed74fa0e3aee13e893c5dfc8
```

The RC annotated tag is:

```text
v1.0.0-rc.1
```

The verified peeled remote tag target is:

```text
b5958edbbf0e3279ed74fa0e3aee13e893c5dfc8
```

Therefore:

```text
approved publication commit
    ==
published RC tag target
```

No tag retargeting is permitted.

---

## 7. Published Artifact Identity

Published wheel:

```text
openprojectlab-1.0.0rc1-py3-none-any.whl
```

Published wheel SHA-256:

```text
0dbea1bdbf972a91c25aeb84e5441cb308df866b269ab8f7feea8d099d93d337
```

Published source distribution:

```text
openprojectlab-1.0.0rc1.tar.gz
```

Published sdist SHA-256:

```text
37e2593a4693b7f038da1b9f0b3ae83643fff2d989992a185a3cdc9022098ea2
```

Published checksum manifest:

```text
SHA256SUMS.txt
```

Published checksum-manifest asset SHA-256 as reported by GitHub:

```text
0b56ca72ab9aec34afabcf3fb00d170522a923d4e0120df3bca6234061bb3c4f
```

The GitHub Release asset set contains exactly:

```text
openprojectlab-1.0.0rc1-py3-none-any.whl
openprojectlab-1.0.0rc1.tar.gz
SHA256SUMS.txt
```

No stale `0.6.0` artifacts or unrelated distributions are part of the RC
publication identity.

---

## 8. Publication Verification

The GitHub Release publication was created draft-first and validated before
publication.

Verified final publication state:

```text
Release tag --- v1.0.0-rc.1
Release title --- OpenProjectLab v1.0.0-rc.1
Target commit --- b5958edbbf0e3279ed74fa0e3aee13e893c5dfc8
Draft --- false
Prerelease --- true
```

Post-publication re-read verification completed successfully.

The published RC is immutable under the accepted release contract:

- do not move `v1.0.0-rc.1`;
- do not replace wheel bytes;
- do not replace sdist bytes;
- do not replace checksum identity under the same RC;
- a materially defective published RC must advance to a new RC identity such
  as `v1.0.0-rc.2`.

---

## 9. Step 8.10.4 — RC Build / Artifact Identity Evidence

Step 8.10.4 established the canonical RC package/release identity and verified
fresh build artifacts.

Historical completion evidence:

```text
Canonical package version --- 1.0.0rc1
Canonical RC tag mapping --- v1.0.0-rc.1
Focused RC build / identity verification --- 70 passed, 0 skipped
Twine check --- Passed
Checksum manifest verification --- Passed
```

Step 8.10.4 artifact bytes remain historical evidence because later accepted
repository changes required fresh artifacts before publication.

---

## 10. Step 8.10.5 — RC Artifact-backed Verification Evidence

Completion-state artifact-backed verification established:

```text
Installed distribution identity --- 1.0.0rc1
Source-checkout isolation --- Passed
Installed opl entry point --- Passed
Packaged runtime resources --- Passed
First 15 Minutes --- Passed
Representative installed-user E2E --- Passed
Integrated release identity --- Passed
Checksum manifest verification --- Passed
Focused completion suite --- 59 passed
Required artifact-backed skips --- 0
```

An initial stale `OPL_RELEASE_COMMIT_SHA` mismatch was correctly rejected by
the fail-closed release identity contract. Fresh artifacts and artifact inputs
were then rebound before completion evidence was accepted.

---

## 11. Step 8.10.6 — Full Regression / Local Quality Gates

Completion-state local evidence:

```text
Full regression --- 1881 passed, 1 deselected
Failures / errors --- 0
Coverage --- 90.90%
Required coverage --- 67.0% --- Passed
Required artifact-backed skips --- 0
git diff --check --- Passed
Ruff --- Passed
Ruff Format --- Passed
pre-commit --- Passed
```

This is the fresh Step 8.10.6 completion-state baseline.

---

## 12. Step 8.10.7 — GitHub Actions / CI

Step 8.10.7 GitHub Actions / CI completed successfully.

Required jobs:

```text
Quality checks --- Passed
Packaging artifact verification --- Passed
```

The Packaging artifact verification job covered build, metadata validation,
SHA-256 generation, artifact resolution, packaging tests, installed-user
documentation readiness, artifact-backed release-readiness, and integrated
package/release identity verification.

---

## 13. Step 8.10.8 — RC Creation / Publication Identity

Step 8.10.8 completed the actual RC publication identity.

Verified publication chain:

```text
approved publication SHA
    b5958edbbf0e3279ed74fa0e3aee13e893c5dfc8

        ↓

annotated tag
    v1.0.0-rc.1

        ↓

fresh verified wheel + sdist
        ↓
SHA256SUMS.txt
        ↓
GitHub Release draft
        ↓
draft identity validation
        ↓
published GitHub prerelease
        ↓
post-publication identity re-read
```

Publication identity verification passed.

---

## 14. Public Contract / Policy / Support Boundary

Formal RC Acceptance must preserve all previously accepted Milestone 8
boundaries:

- Step 8.2 Stable public-contract freeze;
- Step 8.5 EN / zh-TW documentation parity;
- Step 8.6 compatibility and deprecation policy;
- Step 8.7 evidence-backed support matrix and known limitations;
- Step 8.8 release automation / reproducibility;
- Step 8.9 full release-readiness acceptance.

This acceptance record does not expand the Stable public surface, support
matrix, or v1.0 feature scope.

---

## 15. RC vs GA Boundary

Formal acceptance of `v1.0.0-rc.1` must not be interpreted as acceptance of
`v1.0.0` GA.

Current GA state:

```text
v1.0.0 GA Acceptance --- Not Accepted
```

GA remains a later independent gate after actual RC validation.

---

## 16. Completed Closure Gates

All final Step 8.10.9 closure gates are complete:

```text
Acceptance PR --- #154
Acceptance PR CI --- Passed
Acceptance squash merge --- Completed
Acceptance merge commit --- d37a3d84161e66e98ebbff2aafaf1a14e27f865c
main synchronization --- Completed
post-merge consistency verification --- Completed
cross-document terminal-state alignment --- Completed
Formal RC Acceptance --- Accepted
```

No Step 8.10 closure gate remains Pending.

The published RC identity remains unchanged:

```text
RC tag --- v1.0.0-rc.1
Published source SHA --- b5958edbbf0e3279ed74fa0e3aee13e893c5dfc8
```

The acceptance merge commit records repository acceptance evidence only; it
does not retarget the already-published RC.

---

## 17. Formal Acceptance Criteria

The terminal acceptance state is:

```text
Status: Accepted
Formal RC Acceptance: Accepted
```

All of the following conditions have been satisfied:

- this acceptance record has passed review;
- formal acceptance automation passes;
- full required local verification remains green;
- acceptance PR CI passes;
- acceptance PR is squash merged;
- local `main` is synchronized with `origin/main`;
- working tree is clean;
- post-merge consistency verification passes;
- Roadmap, HISTORY, CHANGELOG, governing RC contract, and this record agree on
  the terminal Step 8.10 state;
- `v1.0.0-rc.1` tag target remains unchanged;
- published GitHub prerelease identity remains unchanged;
- `v1.0.0` GA remains unaccepted.

---

## 18. Post-merge Verification Evidence

PR #154 completed the reviewed acceptance-candidate lifecycle and was squash
merged as:

```text
d37a3d84161e66e98ebbff2aafaf1a14e27f865c
```

After synchronization of `main`, post-merge consistency verification and
cross-document terminal-state alignment completed successfully.

Terminal identity relationship:

```text
Published RC source SHA --- b5958edbbf0e3279ed74fa0e3aee13e893c5dfc8
Acceptance merge SHA --- d37a3d84161e66e98ebbff2aafaf1a14e27f865c
RC tag target unchanged --- Yes
GitHub prerelease identity unchanged --- Yes
v1.0.0 GA Accepted --- No
```

---

## 19. Code Review Checklist

### Architecture / Scope

- [x] This record consumes, but does not replace, the governing RC contract.
- [x] Step 8.10 remains separate from v1.0.0 GA Acceptance.
- [x] No new product capability is introduced.
- [x] No accepted Stable/support boundary is expanded.

### Identity

- [x] Package version is `1.0.0rc1`.
- [x] RC identity is `v1.0.0-rc.1`.
- [x] Approved source SHA is full and immutable.
- [x] Published tag target equals approved source SHA.
- [x] Published wheel/sdist names are exact.
- [x] Published SHA-256 values are full lowercase hex digests.
- [x] GitHub Release remains prerelease.
- [x] GitHub Release is no longer draft.
- [x] Published asset set is exact.

### Acceptance Evidence

- [x] Step 8.10.4 evidence is recorded.
- [x] Step 8.10.5 has zero required artifact-backed skips.
- [x] Step 8.10.6 full regression and coverage are recorded.
- [x] Step 8.10.7 CI is recorded.
- [x] Step 8.10.8 publication identity is recorded.
- [x] Pending closure gates are explicit.

### Closure

- [x] Acceptance PR CI passes before acceptance.
- [x] Acceptance PR is merged before acceptance.
- [x] `main` synchronization is complete before acceptance.
- [x] Post-merge consistency is complete before acceptance.
- [x] Cross-document terminal-state alignment is complete before acceptance.
- [x] No closure gate remains Pending when status becomes Accepted.
- [x] GA remains Not Accepted.

---

## 20. Final Decision

Final decision:

```text
Step 8.10 --- RC Acceptance
Status --- Accepted
Formal RC Acceptance --- Accepted
Acceptance PR --- #154
Acceptance merge commit --- d37a3d84161e66e98ebbff2aafaf1a14e27f865c
Published RC source commit --- b5958edbbf0e3279ed74fa0e3aee13e893c5dfc8
v1.0.0 GA Acceptance --- Not Accepted
```

OpenProjectLab `v1.0.0-rc.1` is formally Accepted.

This acceptance closes Milestone 8 RC Acceptance. It does not constitute
`v1.0.0` GA acceptance.
