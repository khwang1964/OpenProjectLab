# v1.3.5-v1.3.7 Read-only Verification Delivery Train Implementation

> Status: Accepted / Completed

## Scope

- v1.3.5 immutable stateless `ReadOnlyVerificationInvoker`
- v1.3.6 strict schema-version-1 request decoder and deterministic JSON/text renderers
- v1.3.7 explicit `release-evidence verify` CLI preview
- bounded UTF-8 input, stable exit statuses, separated stdout/stderr, and disabled prompts
- reuse of the v1.3.4 read-only runtime allowlist without new mutation authority

## Evidence baseline

- Design Train PR #289: merged
- Design Acceptance PR #290: merged as `f1f3f2d52bfe83c23343ba5728ea1f588181966d`
- PR #290 required CI: passed
- PR #290 synchronized-main verification: 28 passed

## Deferred

- implementation acceptance — Accepted / Completed
- stable public SDK, HTTP, RPC, plugin, batch, watch, retry, persistence, and telemetry
- test execution, mutation, commit, push, merge, tag, release, and publication authority

## Code Review Checklist

- [ ] Invocations are immutable, stateless, independent, and exactly once.
- [ ] Request decoding rejects missing, unknown, incorrectly typed, and out-of-range fields.
- [ ] JSON and text output are deterministic and status-consistent.
- [ ] The CLI reads one bounded UTF-8 file and never writes a report file.
- [ ] Exit statuses 0, 1, and 2 preserve the accepted meaning.
- [ ] Credential prompts and arbitrary command execution remain impossible.
- [ ] Focused tests, pre-commit, full regression, and required CI pass.
