interface:
  display_name: "OPL Acceptance Closure"
  short_description: "Close OPL acceptance directly from repository evidence"
  default_prompt: "Use $opl-acceptance-closure to complete the next OPL acceptance closure without a context ZIP."
policy:
  allow_implicit_invocation: true
