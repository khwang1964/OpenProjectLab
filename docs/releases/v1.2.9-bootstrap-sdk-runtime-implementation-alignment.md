# v1.2.9 Bootstrap SDK Runtime Implementation Alignment

> **Status:** Accepted --- Terminally Closed
> **Implementation PR:** #259
> **Merge commit:** `ae2d6908f2e573c6e155a1b6a6991390bf385b57`

## Implemented scope

The minimum `generator.sdk.bootstrap_runtime` adapter now provides the accepted typed
request, result, mode, usage-error, execution-error, and `run_bootstrap` contracts.
It supports preview, apply, and apply-and-validate while delegating to the accepted core
runtime. The SDK remains silent and independent of CLI rendering and exit-code policy.

Existing `generator.sdk` root exports remain unchanged. Validation findings remain normal
invalid results; runtime and check failures fail closed with phase, failed identity, and
completed evidence. Preview and validation do not add filesystem mutation paths.

## Confirmed evidence

```text
Implementation PR --- #259
Merge commit --- ae2d6908f2e573c6e155a1b6a6991390bf385b57
Required implementation CI --- Passed
Synchronized main identity --- Verified
Post-merge focused verification --- 25 passed
```

## Pending closure gates

```text
Terminal-alignment focused tests --- 26 passed
pre-commit --- Passed
Full regression / coverage --- 2519 passed, 56 skipped, 1 deselected / 91.08%
Terminal-alignment PR required CI --- Passed
Synchronized-main verification after alignment merge --- Passed
Implementation acceptance closure --- Accepted
```

Implementation acceptance is declared by the separate terminal closure record.
